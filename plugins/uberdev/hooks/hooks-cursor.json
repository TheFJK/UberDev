{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-start"
      }
    ],
    "userPromptSubmit": [
      {
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/inject-brainstorm-answers"
      }
    ],
    "sessionEnd": [
      {
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-end"
      }
    ],
    "preCompact": [
      {
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/pre-compact"
      }
    ]
  }
}
